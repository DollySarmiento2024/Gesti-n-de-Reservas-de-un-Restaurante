<form method="post" action="{{ path('app_reserva_delete', {'id': reserva.id}) }}" onsubmit="return confirm('¿Estás seguro que quieres eliminar esta reserva?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reserva.id) }}">
    <button class="btn">Borrar reserva {{ reserva.id }}</button>
</form>
